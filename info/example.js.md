**Example:**

```javascript
letterQueue(["PUSH A", "POP", "POP", "PUSH Z", "PUSH D", "PUSH O", "POP", "PUSH T"]) === "DOT"
letterQueue(["POP", "POP"]) === ""
letterQueue(["PUSH H", "PUSH I"]) === "HI"
letterQueue([]) === ""
```